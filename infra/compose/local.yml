version: '3.8'

services:
  app:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    container_name: taskflow_app
    ports:
      - "8080:8080"
    env_file:
      - ../../.env
    volumes:
      - ../../data:/app/data
    restart: unless-stopped
